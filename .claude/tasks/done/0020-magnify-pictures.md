# Problem to solve

The built website in `assets/build` contains plenty of pictures in the pages. Sometimes, the pictures (especially the maps) are a bit small.

This task is about allowing the visitor to click on the image to see the image in big.

Ideally, it should avoid using javascript to avoid long term dependencies.
