# Problem to solve

When retrying the extract_articles, we start again from the pages.yaml. We should start from the articles.yaml.

When we start the script for the first time, we should start from the pages.yaml.
