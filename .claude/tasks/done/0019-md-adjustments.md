# Problem to solve

During the convertion from the Markdown files to the HTML files in `assets/build`, there are some adjustments that must be done to improve the readability of the website.

1. When a link in the Markdown file is `(Lire la suite...)`, it should be simply `Lire la suite`.
2. When there is a link to an `img`, the image should be embedded in the page to avoid an additional click by the visitor.
