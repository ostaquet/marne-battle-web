# Problem to solve

In `relink.py`, the function `relink_page_href()` is performing a relinking of URLs.

There are multiple special URLs that are hard-coded in the Python file. These special URLs should be defined in a YAML for the mapping.
